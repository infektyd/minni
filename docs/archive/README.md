# Archive

Historical, point-in-time documents kept for record — superseded or overtaken by events, not current guidance.

Paths and module names reflect the repo layout at the time of writing (e.g. `engine/` is now `src/minni/`).
