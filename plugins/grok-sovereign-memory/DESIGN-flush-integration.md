# DESIGN: Grok Build Flush / Compact / Dream Integration + Hook Auto-Draft (V1)

See the canonical `~/.agents/DESIGN-sovereign-delivery-layer.md` for the portable boundary, adapter contract, and full Delivery Layer spec. This document describes Grok TUI delivery mechanics only (the thin hook + hybrid notes that sit on top of the canonical SKILL + ritual package).

(The original detailed rationale for the stdlib hook, UserPromptSubmit keyword detection for /flush|compact|dream + distill fallback, SessionStart gauges injection near Layer 1, PreCompact/Stop scar drafting, and guardrails that preserve 100% native behavior lives in the pre-merge history and the sovereignMemory repo `grok/` branch worktree. All portable concepts have been promoted.)

Grok V1 on top of canonical + `~/.agents/artifacts/sovereign-distill-ritual-v1/`. Hooks remain zero-dependency stdlib.