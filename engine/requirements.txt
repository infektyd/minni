# Core
# Python 3.11 or 3.12 recommended for reproducible binary wheels (numpy, faiss-cpu, sentence-transformers).
# Python 3.14 may produce partial installs; use venv + pinned versions below.
sentence-transformers>=5.6.0
numpy>=2.4.6,<3.0

# FAISS (CPU — use faiss-gpu if you have CUDA)
faiss-cpu>=1.14.2

# Cross-encoder re-ranking (included in sentence-transformers)
# No extra install needed

# Token counting for context budgeting
tiktoken>=0.13.0

# File watcher
watchdog>=6.0.0

# Optional: for better SQLite FTS tokenization
# icu4c (system package)
