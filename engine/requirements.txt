# Core
# Python 3.14 is the supported local/CI interpreter. Keep the engine in
# engine/.venv so binary wheels (numpy, faiss-cpu, sentence-transformers) are
# isolated from system site packages.
sentence-transformers>=5.6.0
numpy>=2.5.0,<3.0

# FAISS (CPU — use faiss-gpu if you have CUDA)
faiss-cpu>=1.14.3

# Cross-encoder re-ranking (included in sentence-transformers)
# No extra install needed

# Token counting for context budgeting
tiktoken>=0.13.0

# File watcher
watchdog>=6.0.0

# Testing
pytest
# Coverage visibility (see `make coverage` / engine/.coveragerc). Not wired into
# default pytest addopts so ordinary `pytest` runs stay fast; opt-in via --cov.
pytest-cov>=7.1.0

# Linting (gate: `.venv/bin/python -m ruff check .` from engine/)
ruff>=0.6.0

# Optional: for better SQLite FTS tokenization
# icu4c (system package)
